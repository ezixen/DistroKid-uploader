# DistroKid upload-settings.txt
# One-time settings for DistroKid-uploader (edit anytime; read on every run).
#
# This file is gitignored — it holds your legal name, so it never leaves your PC.
# Template with all options + comments: upload-settings.example.txt
#
# Lines starting with # are comments. Keys are case-insensitive.
#
# ---------------------------------------------------------------------------
# Prices
# ---------------------------------------------------------------------------
album=9.99
track=0.99
#
# ---------------------------------------------------------------------------
# Release identity
# ---------------------------------------------------------------------------
# releaser = Record label / "Released by"
releaser=(your label)
#
# real_name = Legal name → split into First / Middle / Last on DistroKid
#   Jane Q Doe  →  First=Jane  Middle=Q  Last=Doe
# Or set: real_name_first= / real_name_middle= / real_name_last=
real_name=Your Legal Name
#
# artist = Primary artist name on the release (+ Apple Music credits name)
artist=YourArtist
#
# ---------------------------------------------------------------------------
# Track flags
# ---------------------------------------------------------------------------
# instrumental = on | off   (mark tracks as instrumental / no lyrics)
instrumental=on
#
# explicit = on | off
#   off (default) = Explicit lyrics → No  (use this for instrumentals)
#   on            = Explicit lyrics → Yes
explicit=off
#
# ---------------------------------------------------------------------------
# AI disclosure (DistroKid popup: "Which parts of this song were AI-generated?")
# ---------------------------------------------------------------------------
# ai = off | on | both   (see upload-settings.example.txt for every ai_* flag)
ai=off
ai_lyrics=off
ai_music=off
ai_all_audio=off
ai_part_audio=off
ai_part_instruments=off
ai_part_vocals=off
ai_artist_persona=human
#
# ---------------------------------------------------------------------------
# Apple Music credits ("Add credits for each song on this release")
# ---------------------------------------------------------------------------
credit_artist=YourArtist
credit_roles=Unknown, Executive producer
#
# ---------------------------------------------------------------------------
# Stores / free extras
# ---------------------------------------------------------------------------
stores_include_social=on
audiomack=on
release_date=today
#
# ---------------------------------------------------------------------------
# Important checkboxes (mandatory) at bottom of DistroKid form
# ---------------------------------------------------------------------------
# off = leave them for the human to tick
mandatory_checkboxes=off
#
# Genre stays manual. Album title = folder name rule (same as Bandcamp).
